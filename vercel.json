{
  "buildCommand": "npm run build",
  "outputDirectory": "student-panel/dist",
  "installCommand": "cd student-panel && npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
